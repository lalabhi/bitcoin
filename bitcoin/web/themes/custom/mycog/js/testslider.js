(function ($, Drupal) {
    'use strict';

    Drupal.behaviors.slider = {
        attach: function (context, settings) {
            jQuery(document).ready(function($) {
           // alert("trupti");
           //$('')
        });
    },
    };
})(jQuery, Drupal);