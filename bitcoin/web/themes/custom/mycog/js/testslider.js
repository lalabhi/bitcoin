(function ($, Drupal) {
    'use strict';

    Drupal.behaviors.slider = {
        attach: function (context, settings) {
            jQuery(document).ready(function($) {
            alert("hello");
           //$('')
        });
    },
    };
})(jQuery, Drupal);
